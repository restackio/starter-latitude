<script>import { Select as SelectPrimitive } from "bits-ui";
import { theme } from "@latitude-data/client";
import { flyAndScale } from "../../utils";
import { scale } from "svelte/transition";
let className = void 0;
export let sideOffset = 4;
export let inTransition = flyAndScale;
export let inTransitionConfig = void 0;
export let outTransition = scale;
export let outTransitionConfig = {
  start: 0.95,
  opacity: 0,
  duration: 50
};
export { className as class };
</script>

<SelectPrimitive.Content
  {inTransition}
  {inTransitionConfig}
  {outTransition}
  {outTransitionConfig}
  {sideOffset}
  class={theme.ui.select.content.cssClass({ className })}
  {...$$restProps}
>
  <div class={theme.ui.select.content.CONTAINER_CSS_CLASS}>
    <slot />
  </div>
</SelectPrimitive.Content>
