<script context="module">import { theme } from "@latitude-data/client";
</script>

<script>import { onMount } from "svelte";
let ref;
let className = void 0;
export let action = void 0;
export let type = "normal";
export { className as class };
onMount(() => {
  if (!action)
    return;
  action(ref);
});
</script>

<div
  bind:this={ref}
  class={theme.ui.card.contentCssClass({ type, className })}
  {...$$restProps}
>
  <slot />
</div>
