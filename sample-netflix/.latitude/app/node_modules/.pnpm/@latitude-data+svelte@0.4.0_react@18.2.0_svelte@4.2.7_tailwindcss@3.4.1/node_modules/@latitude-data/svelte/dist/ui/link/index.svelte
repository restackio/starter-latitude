<script>import { theme } from "@latitude-data/client";
let className = void 0;
export let to;
export let newTab = false;
</script>

<a
  class={theme.ui.link.cssClass({ className })}
  href={to}
  target={newTab ? "_blank" : undefined}
  rel={newTab ? "noopener noreferrer" : undefined}
  {...$$restProps}
>
  <slot />
</a>
