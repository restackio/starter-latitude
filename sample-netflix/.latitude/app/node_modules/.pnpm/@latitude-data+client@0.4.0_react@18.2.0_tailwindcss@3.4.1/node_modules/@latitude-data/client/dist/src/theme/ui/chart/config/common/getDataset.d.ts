import type { DatasetOption } from 'echarts/types/dist/shared';
import { Dataset } from '../../types';
type BaseSortItem = {
    order: 'asc' | 'desc';
    incomparable?: 'min' | 'max';
    parser?: 'time' | 'number' | 'trim';
};
type SortItem = BaseSortItem & {
    column: string;
};
export type Sort = string | SortItem | SortItem[];
type Props = {
    dataset: Dataset;
    normalizeValues?: boolean;
    sort?: Sort;
};
export declare function getDataset(props: Props): {
    datasets: DatasetOption[];
    datasetIndex: number;
};
export {};
//# sourceMappingURL=getDataset.d.ts.map