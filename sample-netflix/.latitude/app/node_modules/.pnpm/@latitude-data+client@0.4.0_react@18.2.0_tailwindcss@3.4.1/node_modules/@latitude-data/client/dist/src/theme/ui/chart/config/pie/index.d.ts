import { EChartsOption } from 'echarts';
import { Dataset } from '../../types';
import { Sort } from '../common/getDataset';
type ConfigProps = {
    showDecal?: boolean;
    showLegend?: boolean;
    showTotalValue?: boolean;
    showHole?: boolean;
    showLabels?: boolean;
};
export type PieChartProps = {
    dataset: Dataset;
    sort?: Sort;
    displayName?: string;
    animation?: boolean;
    config?: ConfigProps;
};
export default function generatePieConfig({ dataset, sort, displayName, animation, config: { showLabels, showTotalValue, showDecal, showLegend, showHole, }, }: PieChartProps): EChartsOption;
export {};
//# sourceMappingURL=index.d.ts.map