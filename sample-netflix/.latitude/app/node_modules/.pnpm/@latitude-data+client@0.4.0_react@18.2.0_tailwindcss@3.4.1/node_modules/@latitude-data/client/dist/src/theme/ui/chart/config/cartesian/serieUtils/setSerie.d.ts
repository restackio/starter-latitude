import { DBSource } from '../../../types';
import { ChartType, ConfigProps, EchartsCartesianSeriesOption, FullColumn } from '../types';
type Props<T extends ChartType> = {
    dimension: FullColumn;
    serieColumn: string;
    serieDisplayName: string | null;
    chartType: T;
    swapAxis: boolean;
    fields: string[];
    axisIndex: number;
    source: DBSource;
    config: ConfigProps;
    datasetIndex: number;
};
export default function setSerie<T extends ChartType>({ fields, serieColumn, serieDisplayName, chartType, swapAxis, axisIndex, dimension, source, config, datasetIndex }: Props<T>): {
    serie: EchartsCartesianSeriesOption<"line" | "bar" | "area" | "scatter">;
    serieIndexInSource: number;
};
export {};
//# sourceMappingURL=setSerie.d.ts.map