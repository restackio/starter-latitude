import { DBSource } from '../../../types';
import { EchartsCartesianSeriesOption, ChartType, ConfigProps } from '../types';
type Props<T extends ChartType> = {
    serie: EchartsCartesianSeriesOption<T>;
    chartType: ChartType;
    fields: string[];
    source: DBSource;
    config: ConfigProps;
};
export default function setScatterStyle<T extends ChartType>({ serie, chartType, fields, source, config, }: Props<T>): EchartsCartesianSeriesOption<T>;
export {};
//# sourceMappingURL=setScatterStyles.d.ts.map