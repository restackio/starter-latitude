import type { EChartsOption } from 'echarts';
import { Dataset } from '../../types';
import { AnimationEasing } from '../cartesian';
type FunnelDirection = 'ascending' | 'descending';
type FunnelOrientation = 'vertical' | 'horizontal';
export type FunnelChartProps = {
    dataset: Dataset;
    sort?: FunnelDirection;
    orientation?: FunnelOrientation;
    showColorGradient?: boolean;
    showDecal?: boolean;
    showLegend?: boolean;
    showLabels?: boolean;
    animation?: boolean;
    animationEasing?: AnimationEasing;
    animationEasingUpdate?: AnimationEasing;
};
export default function generateFunnelConfig({ dataset, animation, animationEasing, animationEasingUpdate, sort, orientation, showColorGradient, showLabels, showDecal, showLegend, }: FunnelChartProps): EChartsOption;
export {};
//# sourceMappingURL=index.d.ts.map